{
  "name": "semantic-cms",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Semantic CMS — workspace root. Exists to pin this project's first runtime dependency (SCMS-065).",
  "engines": { "node": ">=22" },
  "dependencies": {
    "pg": "8.13.1"
  }
}
