# Spec Health — Semantic CMS

The register of exactly where DESIGN.md is ambiguous enough that a builder would have to
invent an answer — listed so the invention becomes a decision instead. Tags:
**GUESS** (a builder would silently pick), **GAP** (nothing exists), **THIN** (named but
underspecified), **CAVEAT** (specified with a known unresolved tension). Each entry
carries a proposed default where one can be responsibly offered; defaults are proposals,
not decisions, until dispositioned.

Maintained under SCMS-005. Remove entries only by resolving them into DESIGN.md (or a
decision record) — never by deletion.

| # | Tag | Area | The ambiguity | Proposed default |
|---|-----|------|---------------|------------------|
| SH-1 | **PARTIALLY RESOLVED** | Implementation stack | §13 is non-normative; dependencies span TS (Fundamental, SES-adjacent), Rust (rr-rsp, Infinite-Platform precedent), Python (ACP, FPB tooling). Which language owns the resolver, the store adapters, the wire? | TypeScript for resolver + wire + editor (shared client/server/edge per §13); Postgres for Canon; Python retained only for substrate tooling and ACP; Rust deferred until a measured need. | **Owner decided 2026-08-29: migrate off zach-core entirely.** Postgres for Canon, per §13's sketch, landed as a schema with enforcing grants in SCMS-057. Remaining: the adapter, auth (SH-2), and deployment. |
| SH-2 | GUESS | Tenancy & identity | No tenancy model, no identity provider, no session model. Single-tenant (owner properties) or multi-tenant? | Single-tenant first (one owner, many actors incl. agents); actor identity via the four-class identity table (§3.3); external IdP deferred; capability tokens shaped per ICP verification levels. |
| SH-3 | CAVEAT | Deletion vs law | Append-only-forever meets right-to-erasure (GDPR/CCPA) how? Revocation retains provenance — lawful for personal data? | Crypto-shredding candidate: personal-data fields encrypted per-subject; erasure = key destruction, leaving tombstoned ciphertext + intact non-personal provenance. Needs legal review before any third-party personal data enters Canon. |
| SH-4 | GAP | Media & blobs | Canon is record-centric. Images/video/fonts: CAS store, derivative classes (thumbnail/preview/reader-*), codec-outside-identity — described in the IV review, adopted nowhere. | Adopt IV's model: BLAKE3- or SHA-256-addressed CAS, immutable, `Cache-Control: immutable`; manifests declare derivative *classes* never encodings; delivery projections own codecs. |
| SH-5 | THIN | Search | Named as a projection; no query model, no index spec, and the composition of access projection with full-text search (snippet/leak risk) is unaddressed. | Per-access-level index shards built from access-projected inputs only (the fingerprint rule extended to indexes); entitled text never enters an open shard — IV rule 16″ applied to search. |
| SH-6 | GAP | Quantitative budgets | R1–R3 have no numbers: no propagation-latency budget, presence expiry values, resolver p95, commit-wave cadence, degradation tiers. | Starting envelope to be validated by E1 slice: presence expiry 10s; notify fan-out p95 < 2s intra-region; resolver p95 < 10ms per composition; commit waves ≥ 1/s under load; degradation tiers per §8.8 with measured thresholds. |
| SH-7 | THIN | Field plane concretes | Which metrics at launch, which patterns couple what, evidence-floor values — the least concrete plane despite being "ours." | Launch set: attention, heat (recency), staleness (freshness half-life per content class), memory (relation use); one coupling pattern (evidence-resolved shelf morphology); floors per SCMS-002 P13 defaults (0.45 evidence floor, 0.55 name floor) until measured. |
| ~~SH-8~~ | **RESOLVED** | Authoring surface | *Closed by SCMS-031 + SCMS-040:* the editor is a ResolvedSurface whose operations derive from the live contract registry, and its view-model renders publish with different weight from save, every withheld operation with its reason, the four state axes unmerged, and the 25 migration findings on the entries that carry them. Preview published and verified in-browser in both themes. Residual: visual language is deliberately plain and awaits the owner's direction; no edit has been written back through a contract yet (SCMS-041). | — |
| ~~SH-9~~ | **RESOLVED — for the seed corpus** | Import/migration | *Closed by SCMS-028:* `impl/migrate` maps the real 215-entry zach-core archive into Canon, CI-verified against the corpus itself rather than a fixture. Owner-authored content needs no admission gate — it lands as `declared` provenance under project.owner authority. Two residuals, recorded not hidden: (a) the corpus is the **seed-time** markdown state, and reconciliation against the live Neon Postgres system of record needs owner-authorized credentials (SCMS-029, blocked); (b) P1's Admission plane stays undispositioned and becomes required when third-party content arrives (E9, the friends site). | — |
| SH-10 | THIN | Backup & recovery | No RPO/RTO; no operational promises exist yet, so the promise register (P15) has nothing to check. | Declare with the E1 slice: nightly logical backup, RPO 24h → 1h post-MVP, RTO 4h; each promise entered in the register with its verifying artifact. |
| SH-11 | CAVEAT | GPL boundary | open-knowledge consumed as a service (P25) — process-boundary isolation believed sufficient; not yet reviewed by counsel. | Keep strict service boundary (no linking, no vendoring); counsel review before any distribution of a combined work. |
| SH-13 | **DECISION — owner** (narrowed by SCMS-036) | Attestation provenance | *Forgery is closed:* evidence and attestations are Canon records landed through contracts, and neither promotion nor attestation reads its deciding input from the caller. **What remains is policy:** an owner can still record evidence for their own work and attest to it. | Who may evaluate, and what independence each consequence profile demands. `EvidenceRecord.independentEvaluator` already exists and is recorded but not required by any profile — making it required at a stated effect class would be the smallest real change. Needs owner disposition. |
| ~~SH-14~~ | **RESOLVED** | Derived observations are unreachable | *Closed by SCMS-046:* the exclusion was correct and its consequence was not. Observations are signals about subjects, not participants in a surface, so `freeze()` still never makes one a subject — but snapshots now carry a separate `observations` collection with `observationsFor()` as its reader, scoped by the observation's own access. SCMS-028's derived field is reachable again. | — |
| SH-15 | **DECISION — owner** | Migrated publication status | `content.create@1` fixes a new record to draft/unqualified/unpublished and will not read state from input, so **51 entries that were published on the old site arrive unpublished**. This is the system's own claim applied to its migration: publishing is qualification plus promotion, and neither is inherited by copying. It is either exactly right, or an unacceptable amount of ceremony for content that has been live for years. | Three options: (a) keep it strict — re-publish each with real evidence, which is the current behaviour; (b) a distinct owner-authorized `content.adopt` contract that carries prior publication with its provenance recorded as adopted-not-qualified; (c) a migration consequence profile whose obligations are satisfiable by the fact of prior publication. Needs owner disposition. |
| ~~SH-16~~ | **RESOLVED** | The editor bypasses the surface pipeline | *Closed by SCMS-044:* the editor resolves a `ResolvedSurface` with `purpose: "edit"` and reads membership from it; the index resolves a collection surface. Both hand-rolled access comparisons are gone, leaving one implementation of the access rule in the read path. SH-16's own test passes — the authoring surface expresses two materially different ways with identical presented order and exposed operations while morphology differs, so it is not underspecified. | — |
| SH-15 | **GAP — live contradiction** | Drafting republishes | `content.revise@1` builds its next envelope from the prior one, so a revision inherits `publicationState`. Reader surfaces resolve from `journal.current()`, so **an in-progress edit to a promoted subject reaches the public the instant it lands** — verified against landed code, not inferred. This contradicts §6's central claim that publishing is qualification plus promotion and never a boolean: `revise` currently republishes without either. | Defined in SCMS-045: a working copy as an `observed` envelope, so drafting against promoted content cannot move a state axis or enter a reader surface. Awaiting owner scheduling. |
| SH-17 | **CAVEAT — consequence model** | Lag permits publishing | `permits()` allows consequential action in `stale-but-safe`, which is where a lagged client lands. §8.6 qualifies that as permitted **after converge**, and convergence is modelled nowhere — so a client that has been told it is 37 events behind can still promote against a view it knows is stale. Found while building SCMS-035; not fixed there, because whether lag blocks promotion is a consequence-model decision rather than an implementation detail. | **The mechanism now exists** (SCMS-048: `converge()` acknowledges remote activity and leaves the client current, refusing in the four states catching up cannot resolve). What remains is the policy: flip `permits('stale-but-safe','consequential')` to require convergence, or make lag an explicit promotion gate in the consequence profile. One line either way. Needs owner disposition. |
| SH-19 | **CAVEAT — instrument limit** | The leak scan compares identities, not all spellings | After NR-scms-013 the S3 access-constraint check normalizes character references and percent-encoding before scanning, but an identity split across separate attributes or interleaved with markup is still invisible to it. It is a backstop against an adapter leaking by accident — the realistic failure — and not a proof against one encoding deliberately. | Either accept the limit explicitly in the S3 claim wording (current posture), or compare against a parsed DOM rather than a string. |
| ~~SH-20~~ | **RESOLVED as a declared non-property** | Intra-group order is unprotected | *Closed by SCMS-053:* S3 does not promise intra-group order, and now says so. `checkEquivalence` reports `intraGroupOrderMatched` without enforcing it — priority is semantic and its realization is free, so an adapter may legitimately order a group by reading time, alphabet, or nothing at all. What was wrong is that the absence was true by omission: an unwritten missing guarantee is indistinguishable from one people assume they have. A caller who needs the property can now check for it. | — |
| SH-21 | **GAP — migration** | The corpus is unpublished | `content.create@1` refuses to create already-published content (correctly — a caller must not supply the value deciding the gate), so all 215 migrated records arrive `unpublished`, including the 61 that were published in the source. Since SCMS-050 made the read path consult publication, the site correctly renders empty. | Promote those records through the real gate, which requires attestations and therefore intersects SH-13. Part of SCMS-029's reconciliation; not something the importer may do on its own. |
| SH-22 | **THIN — narrowed by SCMS-055** | External link verification blocks articles | Both checkers now exist. `ob/media-alt-text` is fully decidable. `ob/links-resolve` resolves internal references against Canon and reports external URLs as `INCONCLUSIVE`, since they cannot be verified without network access — a coverage gap, so an article containing any external link is still `BLOCKED`. Most of the owner's corpus is Medium imports, so most articles carry external links. | Three options, all the owner's: give the system network access for a link-liveness check; declare that `ob/links-resolve` means internal references only; or remove the obligation from the article profile. Narrowing it unilaterally would be redefining what publication guarantees in order to pass the gate. |
| ~~SH-23~~ | **RESOLVED** | The journal updates rows it may not update | *Closed by SCMS-056:* supersession is read off the successor's `supersedes` pointer and revocation off the receipt chain, so no row is ever rewritten. `deriveIndexes()` recomputes both from append-only data, and a vector asserts the maintained indexes agree with it — an index cannot silently become the source of truth. | — |
| ~~SH-12~~ | **RESOLVED** | Wire protocol concretes | *Closed by SCMS-024:* `schemas/scms/envelope.schema.json` (closed, hash-pattern-typed, freshness rule encoded conditionally) and `schemas/scms/golden/canonicalization.json` (4 positive vectors + 1 negative proving the check can fail). Residual limitation, recorded not hidden: no JSON-Schema library is available under the zero-dependency policy, so the schema document is not meta-validated and schema/runtime agreement is asserted case-wise. |

## Claim register

Claims this project currently makes about itself, with admissible status (ladder:
Documented ≠ Implemented ≠ Tested ≠ Deployed ≠ Verified). A claim may not be stated
above its rung.

| Claim | Rung | Evidence setting the rung | What would strengthen it |
|---|---|---|---|
| The six-plane design is coherent and buildable | **Implemented + Tested — narrow path only** | SCMS-011…019: a slice of every plane exists and composes in one run (impl/e2e, 8 vectors), CI-verified | The same at realistic scale, with durability and transport |
| Substrate gates pass | **Tested (CI-verified)** | Every run since `d6f4bfb` reports success by conclusion, not local exit code (NR-scms-002 corrected the prior false claim) | Gate self-tests staying green as gates grow |
| Review findings accurately describe the sources | **Partially verified** | scms-evidence-005: 16/20 load-bearing claims confirmed by adversarial pass, 4 partial, 0 refuted | Verification of the remaining claims |
| Dependencies are pinned and consumable | **Documented + hash-locked** | All pins content-digested (alignment record); SES and FPB re-pinned upstream-first | Admission scans (P17) per pin; UD-8/UD-9 closed |
| Surface resolution is access-safe | **Implemented + Tested** | SCMS-008/014: hidden state never enters a dependency set, and cannot move an output, trace, or invalidation (byte-identical results) | Adversarial review of the access boundary by an independent party |
| One surface can be expressed two materially different ways | **S3 conformance (narrow path)** | SCMS-009: 10 vectors, six preserved properties, morphology provably differing | S4 — and S4 must run **outside** this repository (SCMS-010) |
| Publishing is qualification **plus** promotion, never a boolean | **Implemented + Tested — with one claim corrected and one gap open** | SCMS-013/020: promotion refuses non-qualified, under-verified, and mismatched candidates; compensation exists and round-trips. **This row previously also claimed 'unauthorised'. That was false** — NR-scms-005 showed no handler authorized anything, and an anonymous caller could promote an owner-private draft. Authorization now exists (SCMS-031) and is tested by reason, not just by outcome | Attestations a caller cannot write for themselves (scms-blocker-003 / SH-13). **SCMS-050 corrected a larger gap: until then the read path never consulted publication at all, so this entire gate had no effect on what a reader saw** |
| No persistent mutation outside a registered contract | **Implemented + Tested (source-level), and authorized as of SCMS-031** | SCMS-012 write path; SCMS-023 boundary gate with a self-test proving it can fail; SCMS-031 adds the authority gate the write path had been missing entirely (NR-scms-005), and NR-scms-006 corrects that gate after an adversarial pass found it failing OPEN on prototype-chain keys | Runtime capability enforcement, not just source-level |
| Declared content types are enforced | **Implemented + Tested (where declared)** | SCMS-022: the governed write consults the declared type; opt-in, because types that do not exist are not pretended to exist | A declared type for every content kind in use |
| R1 live co-authoring | **Partially established** | `required` lane proven (SCMS-012), `bounded` lane proven (SCMS-021); **free lane NOT built** — blocked on P7/P22 | Owner disposition of P7/P22, then the free lane |
| R2 live propagation | **Partially established — emission and wire, no real connection** | SCMS-032 outbox; SCMS-033/034 wire, replay and lag; SCMS-035 derived freshness. **SCMS-038 found and fixed two defects here**: the cursor leaked exact global write volume, and a malformed cursor caused silent total event loss | A real connection under latency; durability (SH-1); and a measured propagation budget (SH-6) |
| R3 live honesty | **Implemented + Tested (semantic core), and now derived rather than asserted** | SCMS-017: six states, the drafting/consequential asymmetry, presence expiring by construction. SCMS-035: the chip's freshness is derived from actual transport deliveries — a client with no delivery cannot render `live`. **NR-scms-010 corrected the editor, which had been claiming `live · checked 0s ago` while holding no subscription at all** | Freshness under real latency on a real connection; and SH-17, since a lagged client can still publish |
| Real content maps into Canon without collapsing what the source keeps | **Implemented + Tested against a real corpus** | SCMS-028: 12 vectors over the actual 215-entry archive — unlisted preserved as itself, private content at owner access, model-generated field kept as a separate `derived` envelope, and the source's own status collapse surfaced as 22 findings rather than inherited | The same against the live Postgres state (SCMS-029) — the reader round-trip is now closed by SCMS-030 |
| The site can be a reader expression of Canon rather than a content client | **Implemented + Tested over a real corpus** | SCMS-030: the real /writing and /work routes resolve as surfaces over the migrated archive; feed and sitemap derive from the page's own ResolvedSurface, so a feed cannot disagree with its page; unlisted content is reachable by link and in no index | Wiring the declarations into the site's renderer, and a deployment |
| An editor cannot offer an affordance nothing implements | **Implemented + Tested** | SCMS-031: operations are derived from the live contract registry, unimplemented intents are withheld with a stated reason, and a compensatable operation is not offered while its compensation is unregistered | A rendered editor, and the visual half of SH-8 |
| An author can see the system's honesty, not just be told about it | **Implemented + Tested** | SCMS-040: the chip flips to `local · unsent` on the first keystroke, publishing stays refused with its reason visible while no attestation covers the revision, and conflict keeps typing open while freezing publication | A person actually using it (SCMS-041) |
| Content can only come into existence through a contract | **Implemented + Tested** | SCMS-041: `content.create@1` exists, the whole corpus enters through it with zero direct appends, the import is idempotent, and a created record cannot arrive published or qualified however the input is shaped | The same under concurrency, and a persistence engine (SH-1) |
| Editing content does not destroy it | **Implemented + Tested** | SCMS-042 / NR-scms-008: a slot-level edit preserves its siblings, removal is explicit rather than by omission, and the receipt names the exact field that moved. Before this, editing an article's body silently deleted its title and summary while reporting success | Vectors for every write path against realistically-nested bodies, not flat fixtures |
| No gate reads its deciding input from the caller | **Implemented + Tested** | SCMS-036: evidence and attestations are Canon records; a forged disposition is not read, forged evidence is not consulted, and a borrowed attestation is not expressible | Evaluator independence (SH-13), which is policy rather than repair |
| A person can actually edit through the system | **Implemented + verified by interaction** | SCMS-043: the editor runs, imports through `content.create@1`, saves through `content.revise@1`, and records a P7 observation per edit. Verified by making an edit in a browser, not by vectors — a lower rung than the rest of this register | Vectors over the server; the owner's own editing session; and routing it through the surface pipeline it currently bypasses (SH-16) |
| The site is an expression of Canon, not a client of it | **Implemented + Tested** | SCMS-037: the reader-web renderer is a third expression adapter held to the S3 equivalence contract against the voice expression of the same surface — identical members, order and operation exposure, differing morphology. Real pages, feeds and sitemap over the real corpus, with no private entry reachable | A deploy, caching and invalidation, and a measured render budget |
| A person can take content from draft to published, and a reader sees it | **Implemented + verified by interaction** | SCMS-051: qualify and promote routes in the editor, and the site replays owner actions through the same contracts. Driven end to end on the real corpus — a promoted public article reaches `/writing` and RSS; a promoted private one reaches neither | Vectors over the HTTP routes (SCMS-052 covered the replay half), shared durable Canon (SH-1), and an independent evaluator (SH-13) |
| The published artifacts match their canonical sources | **Tested** | scripts/check-projection-sync.py in CI, with a self-test | — |
| The system is empirically useful | **NOT established** — but no longer untested against reality | SCMS-028: a real 215-entry corpus maps into Canon and resolves, and the mapping found a genuine modelling collapse in the owner's live data (dual-vocabulary `status`, 22 findings). That establishes the *mapping*, not usefulness: nothing is deployed, no human has used the result, rcp-001 remains an active obligation | A person doing real work through it — which needs the authoring surface (E12/SH-8) and a deployment |

*"This register narrows claims; it does not manufacture validation."*
