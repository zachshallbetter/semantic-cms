# ACP gateway. Copy to .env.local (git-ignored). Never commit the token.
ACP_GATEWAY_URL=https://acp-gateway-production.up.railway.app
ACP_GATEWAY_TOKEN=<from the acp-gateway deployment secret store>
