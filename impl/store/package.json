{
  "name": "@scms/store",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Canon in Postgres (SCMS-057): DESIGN.md \u00a713's sketch made real \u2014 append-only by grant, emission by trigger, current and revoked derived as views. The conformance suite proves the database refuses, rather than trusting that it would.",
  "scripts": {
    "test": "./test/run.sh && node --test test/adapter.test.ts"
  }
}
